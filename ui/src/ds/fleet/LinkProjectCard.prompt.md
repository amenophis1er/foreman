The only way to add work to Foreman. Sits as the final cell in the fleet grid, never as a header button.

```jsx
<LinkProjectCard onClick={() => setPicker(true)} />
```

A 22px Plus icon over the label `Link a project` and the hint `or drop a folder anywhere`. Dragging a folder onto the fleet is the second path to the same action.
