Labels a rail section or panel group; Foreman has no other heading level in the body.

```jsx
<SectionTitle>Approvals</SectionTitle>
```

Written as a plain lowercase-in-source noun (`Approvals`, `Runs`, `Plan`) and rendered uppercase by CSS. One or two words; never a sentence.
